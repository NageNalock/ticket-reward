name: Build and release macOS app

on:
  workflow_dispatch:
  push:
    branches:
      - "**"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: macos-14
    timeout-minutes: 45

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m venv .venv
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/python -m pip install -r requirements-build.txt

      - name: Run tests and lint
        run: |
          .venv/bin/python -m unittest discover -s tests -v
          .venv/bin/ruff check src tests scripts

      - name: Build ad-hoc signed Apple Silicon app
        run: bash scripts/build_app.sh

      - name: Verify bundle and prepare checksums
        run: |
          test "$(uname -m)" = "arm64"
          test "$(plutil -extract CFBundleShortVersionString raw 'dist/Bing Rewards.app/Contents/Info.plist')" != ""
          file 'dist/Bing Rewards.app/Contents/MacOS/Bing Rewards' | grep -q 'arm64'
          codesign --verify --deep --strict 'dist/Bing Rewards.app'
          cd dist
          shasum -a 256 Bing-Rewards-macOS-arm64.zip > SHA256SUMS.txt

      - name: Publish GitHub Release
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="$(.venv/bin/python -c 'import json; print(json.load(open("build/build-info.json"))["release_tag"])')"
          gh release create "$TAG" \
            dist/Bing-Rewards-macOS-arm64.zip \
            dist/SHA256SUMS.txt \
            --target "$GITHUB_SHA" \
            --latest \
            --title "Bing Rewards $TAG" \
            --generate-notes
